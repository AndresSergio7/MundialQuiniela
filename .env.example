# ============================================================
# MUNDIAL QUINIELA - Environment Variables
# Copy this file to .env and fill in the values.
# Never commit .env to version control.
# ============================================================

# ---- Supabase ----
EXPO_PUBLIC_SUPABASE_URL=https://gelmnydchiiohryyxovv.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_61v_3SPzn1t3cWryGbXGGg_mmH61B9k

# URL absoluta de recuperación de contraseña (opcional).
# Si no la defines, en web se usa origin actual + /reset-password.
# Debe coincidir EXACTAMENTE con una entrada en Supabase:
# Authentication → URL configuration → Redirect URLs
# Ejemplo dev: http://localhost:8081/reset-password
# EXPO_PUBLIC_AUTH_RECOVERY_REDIRECT_URL=

# ---- Football Data API (optional — mock used if missing) ----
EXPO_PUBLIC_FOOTBALL_API_URL=https://api.football-data.org/v4
EXPO_PUBLIC_FOOTBALL_API_KEY=92b8d95a143047a0a74b437c653bf92c

# ---- EAS (Expo Application Services) ----
EXPO_PUBLIC_EAS_PROJECT_ID=your-eas-project-id

# ---- Supabase CLI (migrations desde terminal) ----
# Project ref de Supabase (Dashboard -> Project Settings -> General)
SUPABASE_PROJECT_REF=gelmnydchiiohryyxovv

# DB password del proyecto remoto (Database password)
# Solo necesaria para `npm run db:push` no interactivo
SUPABASE_DB_PASSWORD=your-db-password
